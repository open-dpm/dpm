# ═══════════════════════════════════════════════════════════════════════════
# CANONICAL ENTITY: payment (v1)
# ═══════════════════════════════════════════════════════════════════════════
# Canonical definition of a payment against an order.

kind: "canonical_entity"

spec_version: "1.0.0"
manifest_version: "1.0.0"
status: "active"  # draft | active | deprecated | retired

metadata:
  name: "payment"
  namespace: "canonical"
  display_name: "Payment"
  description: |
    Canonical definition of a payment: one row is one payment attempt against
    an order. Products whose rows are payments conform to this entity.

  owner:
    team: "data-architecture"
    email: "data-architecture@example.com"
    mattermost: "data-architecture"

  tags:
    - "canonical"
    - "transactional-data"

  created_at: "2026-03-01"
  updated_at: "2026-03-01"
  created_by: "data-architecture@example.com"

schema:
  file: "./schema.avsc"
  format: "avro"

changelog:
  - version: "1.0.0"
    date: "2026-03-01"
    author: "data-architecture@example.com"
    changes:
      - type: "initial"
        description: "First canonical definition of the payment entity"
    breaking: false
